# 🔓 CORS Setup Guide - NoCodeBackend

## ปัญหาที่พบ

```
CORS Error: Failed to fetch
```

เว็บไซต์ของคุณไม่ได้รับอนุญาตให้เรียก API จาก NoCodeBackend

## วิธีแก้ไข (ทำใน NoCodeBackend.com)

### ขั้นตอนที่ 1: เข้าสู่ NoCodeBackend

1. เปิดเบราว์เซอร์ใหม่
2. ไปที่ https://nocodebackend.com
3. Login เข้าสู่ระบบ

### ขั้นตอนที่ 2: เข้าโปรเจกต์

1. หาโปรเจกต์ชื่อ **"tourwow"** หรือ **"54566_tourwow"**
2. คลิกเข้าไป

### ขั้นตอนที่ 3: เปิดหน้า Secret Keys

1. มองหาเมนูด้านซ้าย หรือด้านบน
2. คลิกที่ **"Settings"** หรือ **"API Keys"** หรือ **"Secret Keys"**
3. จะเห็นหน้าที่มี API Key แสดงอยู่

### ขั้นตอนที่ 4: ตั้งค่า Allowed Domains

ในหน้า Secret Keys จะมีส่วนที่เขียนว่า:

```
┌─────────────────────────────────────────────┐
│ Secret keys                            ✕    │
├─────────────────────────────────────────────┤
│ Instance: 54566_tourwow                     │
│                                             │
│ Keys                                        │
│ Create new key                              │
│                                             │
│ system23233d7b3c86c97bf30d6ad51bc5072...   │
│ Scopes: all                                 │
│ Copy    Delete                              │
│                                             │
│ Allowed domains (supports *.example.com)    │ 👈 ตรงนี้!
│ ┌─────────────────────────────────────────┐ │
│ │                                         │ │ 👈 ช่องนี้!
│ └─────────────────────────────────────────┘ │
│                                             │
│                          Save               │ 👈 ปุ่มนี้!
└─────────────────────────────────────────────┘
```

**ในช่องว่างนั้น ให้พิมพ์:**

```
*
```

(เครื่องหมายดอกจัน 1 ตัว)

### ขั้นตอนที่ 5: บันทึก

1. คลิกปุ่ม **"Save"** ด้านล่าง
2. รอสักครู่ให้ระบบบันทึก

### ขั้นตอนที่ 6: ทดสอบ

1. กลับมาที่หน้า `test-cors.html` ที่เปิดอยู่
2. กด **Refresh** (Cmd+R หรือ F5)
3. ดูผลลัพธ์

## ถ้าไม่เจอช่อง "Allowed domains"

ลองหาในที่เหล่านี้:

1. **Settings** > **CORS Settings**
2. **API Configuration** > **Allowed Origins**
3. **Security** > **CORS**
4. **Database Settings** > **Access Control**

## ทางเลือกอื่น

ถ้าไม่เจอช่อง Allowed domains เลย อาจต้อง:

1. **อัพเกรดแพ็คเกจ** - บางแพ็คเกจไม่มี CORS settings
2. **ติดต่อ Support** - support@nocodebackend.com
3. **ใช้ Proxy Server** - สร้าง backend ตัวกลาง

## ค่าที่แนะนำสำหรับ Allowed Domains

### สำหรับ Development (ทดสอบ)
```
*
```
หรือ
```
file://
localhost
127.0.0.1
```

### สำหรับ Production (จริง)
```
yourdomain.com
*.yourdomain.com
```

## ตรวจสอบว่าตั้งค่าสำเร็จ

หลังจาก Save แล้ว ให้:

1. เปิด `test-cors.html`
2. ดูผลลัพธ์:
   - ✅ สีเขียว = สำเร็จ
   - ❌ สีแดง = ยังไม่ได้

## ยังไม่ได้?

ถ้าตั้งค่าแล้วยังไม่ได้ ให้:

1. **รอ 1-2 นาที** - บางครั้งต้องรอให้ระบบอัพเดท
2. **Clear Cache** - ลบ cache ของเบราว์เซอร์
3. **ลอง Browser อื่น** - ลอง Chrome, Safari, Firefox
4. **ตรวจสอบ API Key** - ดูว่า API Key ถูกต้องหรือไม่

## ติดต่อ

หากยังมีปัญหา:
- Email: support@nocodebackend.com
- Documentation: https://docs.nocodebackend.com
