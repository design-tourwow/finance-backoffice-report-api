# 🔑 API Key Setup Guide

## ปัญหาที่พบ

```
Error: 403 Forbidden - Invalid secret_key
```

API Key ที่ใช้ในไฟล์ `config.js` ไม่ถูกต้อง

## วิธีแก้ไข

### ขั้นตอนที่ 1: หา API Key จาก NoCodeBackend

1. ไปที่ [NoCodeBackend.com](https://nocodebackend.com)
2. Login เข้าสู่ระบบ
3. เลือกโปรเจกต์ `54566_tourwow`
4. ไปที่เมนู **Settings** หรือ **API Keys** หรือ **Credentials**
5. หา **API Key** หรือ **Secret Key** หรือ **Bearer Token**
6. Copy API Key

### ขั้นตอนที่ 2: อัพเดท config.js

เปิดไฟล์ `config.js` และแก้ไขบรรทัดนี้:

```javascript
const CONFIG = {
  API_BASE_URL: 'https://openapi.nocodebackend.com',
  INSTANCE_ID: '54566_tourwow',
  API_KEY: 'YOUR_ACTUAL_API_KEY_HERE',  // 👈 แทนที่ตรงนี้
  // ...
};
```

### ขั้นตอนที่ 3: ทดสอบอีกครั้ง

1. เปิดไฟล์ `test-api.html` ในเบราว์เซอร์
2. คลิกปุ่ม **Test Full URL Test**
3. ควรเห็นผลลัพธ์สีเขียว (200 OK)

## ตัวอย่าง API Key ที่ถูกต้อง

API Key ควรมีรูปแบบประมาณนี้:
- `sk_live_xxxxxxxxxxxxxxxxxxxxx`
- `Bearer xxxxxxxxxxxxxxxxxxxxx`
- `xxxxxxxxxxxxxxxxxxxxx` (string ยาวๆ)

## หมายเหตุ

⚠️ **อย่าแชร์ API Key ของคุณกับใครก็ตาม!**

API Key เป็นข้อมูลลับที่ใช้ในการเข้าถึง API ของคุณ

## ตรวจสอบ API Key

หลังจากอัพเดท API Key แล้ว ให้รันคำสั่งนี้ใน Console:

```javascript
console.log('API Key:', CONFIG.API_KEY.substring(0, 20) + '...');
```

## ถ้ายังไม่ได้

ลองตรวจสอบ:

1. **Base URL ถูกต้องหรือไม่?**
   - ตรวจสอบว่าเป็น `https://openapi.nocodebackend.com` จริง
   
2. **Instance ID ถูกต้องหรือไม่?**
   - ตรวจสอบว่าเป็น `54566_tourwow` จริง

3. **API Key หมดอายุหรือไม่?**
   - ลองสร้าง API Key ใหม่จาก NoCodeBackend

4. **CORS ถูกตั้งค่าหรือไม่?**
   - ตรวจสอบว่า NoCodeBackend อนุญาต CORS จาก domain ของคุณ

## ติดต่อ Support

หากยังมีปัญหา ติดต่อ NoCodeBackend Support:
- Email: support@nocodebackend.com
- Documentation: https://docs.nocodebackend.com
