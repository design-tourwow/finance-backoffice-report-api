# 🚀 Vibe Code Team Workflow: 3 คนทำงานพร้อมกัน

## 📋 ภาพรวม

Workflow นี้แสดงให้เห็นว่า **GAP, Por, และ Cherry** สามารถทำงานพร้อมกันได้อย่างไร โดยไม่กระทบงานของกันและกัน

---

## 👥 สมาชิกในทีม

### 👨‍💻 GAP
- **รับผิดชอบ:** Report A
- **ไฟล์:** `report-a.js`
- **Branch:** `feature/gap-report-a`
- **สี:** 🔵 สีน้ำเงิน

### 👨‍💻 Por
- **รับผิดชอบ:** Report B
- **ไฟล์:** `report-b.js`
- **Branch:** `feature/por-report-b`
- **สี:** 🟠 สีส้ม

### 👩‍💻 Cherry
- **รับผิดชอบ:** Report C
- **ไฟล์:** `report-c.js`
- **Branch:** `feature/cherry-report-c`
- **สี:** 🔴 สีแดง

---

## 🔄 Workflow ทั้ง 7 ขั้นตอน (เหมือนกันทุกคน)

### 1️⃣ Pull Staging
```bash
git checkout staging
git pull origin staging
```
**ความหมาย:** ดึงโค้ดล่าสุดจาก Staging  
**ทำไม:** เพื่อให้โค้ดในเครื่องเราเท่ากับเพื่อน

### 2️⃣ สร้าง Branch
```bash
# GAP
git checkout -b feature/gap-report-a

# Por
git checkout -b feature/por-report-b

# Cherry
git checkout -b feature/cherry-report-c
```
**ความหมาย:** สร้างห้องทำงานส่วนตัว  
**ทำไม:** เพื่อไม่กระทบงานคนอื่น

### 3️⃣ เขียนโค้ด (ใช้ Vibe Code)
```
แก้ไฟล์ของตัวเอง:
- GAP แก้ report-a.js
- Por แก้ report-b.js
- Cherry แก้ report-c.js
```
**ข้อควรระวัง:**
- ❌ ห้าม AI แก้ไฟล์ Shared Code เอง
- ❌ ห้าม AI แก้ไฟล์คนอื่นโดยไม่บอก

### 4️⃣ Commit
```bash
git add .
git commit -m "Add Report A feature"
```
**ความหมาย:** บันทึกจุด Save  
**ทำไม:** ยืนยันว่างานชิ้นนี้เสร็จแล้ว

### 5️⃣ Push (Sync + Merge + Push)
```bash
# วิธีที่ 1: ใช้ Script (แนะนำ)
./scripts/safe-push.sh

# วิธีที่ 2: Manual
git checkout staging
git pull origin staging
git checkout feature/gap-report-a
git merge staging
git push origin feature/gap-report-a
```
**💡 จุดสำคัญ:** ต้อง Sync ก่อนส่ง!
1. ดึงงานเพื่อนมา (Pull Staging)
2. ผสมกับงานเรา (Merge Staging)
3. ส่งขึ้น Server (Push)

### 6️⃣ สร้าง Pull Request
1. เปิด GitHub
2. คลิก "Compare & pull request"
3. เลือก Base: `staging` ← Compare: `feature/gap-report-a`
4. กรอกรายละเอียด
5. คลิก "Create pull request"

**ความหมาย:** ส่งการบ้านให้ตรวจ  
**ทำไม:** เพื่อให้ Team Lead รีวิวก่อนรวมโค้ดจริง

### 7️⃣ Merge (Team Lead ทำ)
1. Team Lead รีวิวโค้ด
2. ถ้าผ่าน → คลิก "Merge pull request"
3. งานของคุณจะไปโผล่ที่ Staging Server

**ความหมาย:** รวมงานเข้ากองกลาง  
**ผลลัพธ์:** งานของคุณจะไปโผล่ที่ Staging Server

---

## 💡 Timeline ตัวอย่าง: วันหนึ่งของทีม

### เช้า (09:00 - 09:10)
```
09:00 - GAP, Por, Cherry ทั้ง 3 คน Pull Staging พร้อมกัน
        ✅ ทุกคนได้โค้ดเวอร์ชันเดียวกัน

09:05 - แต่ละคนสร้าง Branch ของตัวเอง
        🔵 GAP: feature/gap-report-a
        🟠 Por: feature/por-report-b
        🔴 Cherry: feature/cherry-report-c

09:10 - เริ่มเขียนโค้ดพร้อมกัน
        ⚡ ทำงานพร้อมกัน ไม่รบกวนกัน
```

### สาย (10:00 - 11:00)
```
10:00 - 🔵 GAP เสร็จก่อน
        ✅ Commit → Push → PR → Merge
        📦 Staging มี Report A แล้ว

10:30 - 🟠 Por เสร็จ
        🔄 Commit → Push (ดึงงาน GAP มาด้วย) → PR → Merge
        📦 Staging มี Report A + B แล้ว

11:00 - 🔴 Cherry เสร็จ
        🔄 Commit → Push (ดึงงาน GAP + Por มาด้วย) → PR → Merge
        📦 Staging มีงานครบทั้ง 3 Report!
```

### ผลลัพธ์
```
✅ Staging มีงานครบทั้ง 3 Report
✅ ไม่มี Conflict เพราะแต่ละคนแก้ไฟล์ต่างกัน
✅ ทุกคนได้ทำงานพร้อมกัน ประหยัดเวลา
```

---

## 🎯 จุดเด่นของ Workflow นี้

### ✅ ทำงานพร้อมกัน (Parallel)
- 3 คนทำงานพร้อมกันได้โดยไม่รบกวนกัน
- ไม่ต้องรอคนอื่นทำเสร็จก่อน
- ประหยัดเวลาได้มาก

### ✅ แยกงานชัดเจน
- แต่ละคนรับผิดชอบไฟล์ต่างกัน
- ลดโอกาส Conflict
- รู้ว่าใครทำอะไร

### ✅ ป้องกัน Conflict
- ใช้ Branch แยกกัน
- Sync ก่อน Push เสมอ
- ใช้ safe-push.sh ช่วยอัตโนมัติ

### ✅ มี Code Review
- ต้องผ่าน Pull Request ก่อน Merge
- Team Lead ตรวจสอบโค้ด
- ป้องกันโค้ดผิดพลาด

### ✅ ปลอดภัย
- Branch Protection ป้องกัน Push ตรง Staging
- CODEOWNERS ป้องกันแก้ไฟล์ผิดคน
- มี History ย้อนกลับได้

### ✅ ง่ายต่อการ Rollback
- ถ้าพังสามารถย้อนกลับได้
- แต่ละ Commit มี History
- ไม่กระทบงานคนอื่น

---

## ⚠️ สิ่งที่ต้องระวัง

### 1. Pull ก่อนเสมอ
```bash
# ❌ ผิด: เริ่มทำงานทันที
git checkout -b feature/my-work

# ✅ ถูก: Pull ก่อน
git checkout staging
git pull origin staging
git checkout -b feature/my-work
```

### 2. Sync ก่อน Push
```bash
# ❌ ผิด: Push ตรงๆ
git push origin feature/my-work

# ✅ ถูก: Sync ก่อน
./scripts/safe-push.sh
```

### 3. ห้ามแก้ Shared Code
```
❌ ห้าม: แก้ shared/auth.js เอง
✅ ถูก: แจ้ง Team Lead ก่อน
```

### 4. ห้าม Push ตรง Staging
```bash
# ❌ ห้ามทำ!
git checkout staging
git commit -m "Fix bug"
git push origin staging  # จะโดน Branch Protection บล็อก

# ✅ ถูก: ใช้ Branch + PR
git checkout -b feature/fix-bug
git commit -m "Fix bug"
git push origin feature/fix-bug
# สร้าง PR บน GitHub
```

### 5. Test ก่อน Push
```bash
# ✅ ทดสอบก่อนเสมอ
npm test
npm run build

# แล้วค่อย Push
git push origin feature/my-work
```

---

## 🔥 กรณีมี Conflict (งานชนกัน)

### สถานการณ์
```
GAP และ Por แก้ไฟล์เดียวกัน (shared/utils.js)
```

### วิธีแก้
```bash
# 1. คนที่ Push ทีหลัง (Por) จะเจอ Conflict
git push origin feature/por-report-b
# Error: Conflict detected!

# 2. Git จะบอกว่าไฟล์ไหนชน
git status
# Conflict in: shared/utils.js

# 3. เปิดไฟล์ → เลือกว่าจะเอาโค้ดของใคร
# ไฟล์จะมีลักษณะนี้:
<<<<<<< HEAD
// โค้ดของ GAP
=======
// โค้ดของ Por
>>>>>>> feature/por-report-b

# 4. แก้ไขให้เหลือโค้ดที่ต้องการ
# 5. Commit อีกครั้ง
git add shared/utils.js
git commit -m "Resolve conflict with GAP's changes"

# 6. Push ใหม่
git push origin feature/por-report-b
```

### 💡 Tips: ป้องกัน Conflict
```bash
# ใช้ safe-push.sh จะช่วยดึงงานคนอื่นมาก่อนอัตโนมัติ
./scripts/safe-push.sh

# Script จะทำให้:
# 1. Pull Staging ล่าสุด
# 2. Merge เข้า Branch ของคุณ
# 3. ถ้ามี Conflict จะบอก ให้แก้ก่อน
# 4. Push ขึ้น Server
```

---

## 📊 ดู Workflow แบบ Visual

### วิธีที่ 1: ใช้ Diagrams.net
1. เปิด https://app.diagrams.net/
2. คลิก "Open Existing Diagram"
3. เลือกไฟล์ `workflowxml.md`
4. จะเห็น Workflow ของทั้ง 3 คนแบบ Visual

### วิธีที่ 2: เปิด HTML Viewer
```bash
# เปิดไฟล์นี้ใน Browser
open team-workflow-diagram.html
```

---

## 📚 เอกสารเพิ่มเติม

- **[FINAL_WORKFLOW.md](FINAL_WORKFLOW.md)** - Workflow Guide ฉบับสมบูรณ์
- **[QUICK_REFERENCE.md](QUICK_REFERENCE.md)** - คำสั่งที่ใช้บ่อย
- **[CODEOWNERS_QUICKSTART.md](CODEOWNERS_QUICKSTART.md)** - CODEOWNERS Guide
- **[SHARED_CODE_WORKFLOW.md](SHARED_CODE_WORKFLOW.md)** - วิธีจัดการ Shared Code
- **[POTENTIAL_ISSUES.md](POTENTIAL_ISSUES.md)** - ปัญหาที่อาจเจอและวิธีแก้

---

## 🎓 สรุป

### แนวคิดสำคัญ
1. **Branch = ห้องทำงานส่วนตัว** - ทำอะไรก็ได้ ไม่กระทบคนอื่น
2. **Staging = กองกลาง** - ที่รวมงานของทุกคน
3. **Pull Request = ส่งการบ้าน** - ให้ Lead ตรวจก่อน
4. **Merge = รวมงาน** - เอางานของคุณไปรวมกับคนอื่น
5. **Sync = ซิงค์งาน** - ดึงงานล่าสุดมาผสมก่อน Push

### Best Practices
1. ✅ **แบ่งงานชัดเจน** - แต่ละคนรับผิดชอบไฟล์ต่างกัน
2. ✅ **Pull บ่อยๆ** - ก่อนเริ่มงานใหม่ ดึงโค้ดล่าสุดเสมอ
3. ✅ **Commit เล็กๆ** - อย่ารอทำเสร็จหมดค่อย Commit
4. ✅ **แจ้งทีม** - ถ้าจะแก้ Shared Code ต้องบอกก่อน
5. ✅ **ใช้ Branch** - อย่า Push ตรงไป Staging เด็ดขาด
6. ✅ **Review PR** - ตรวจงานกันก่อน Merge
7. ✅ **Test ก่อน Push** - ทดสอบให้แน่ใจว่าโค้ดไม่พัง

---

## 🤝 ต้องการความช่วยเหลือ?

- ถามใน Team Chat
- ดูเอกสารใน `FINAL_WORKFLOW.md`
- ใช้ Script ใน `scripts/` folder
- ดู CODEOWNERS ว่าใครรับผิดชอบไฟล์ไหน

---

**สร้างโดย:** Vibe Code Team  
**อัพเดทล่าสุด:** 2 มกราคม 2026  
**เวอร์ชัน:** 1.0
